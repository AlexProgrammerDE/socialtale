export interface UserSettings {
  name: string;
  email: string;
  emailNotifications: boolean;
}

export interface UIData {
  name: string;
  avatar: string;
  theme: string;
  inboxCount: number;
}
